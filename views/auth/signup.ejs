<%- include('../partials/header') %>
</head>
<body class="bg-gradient-to-r from-pink-50 via-red-50 to-red-100 min-h-screen">
  <!-- ✅ Navbar -->
  <%- include('../partials/nav') %>

  <!-- ✅ Main content -->
  <main class="flex items-center justify-center min-h-screen pt-20 px-4">
    <div class="w-full max-w-lg bg-white/80 backdrop-blur-lg rounded-3xl shadow-2xl p-10 border border-red-100">
      
      <!-- Title -->
      <h1 class="text-4xl font-extrabold text-center text-red-600 mb-3">Create Account</h1>
      <p class="text-center text-gray-500 mb-8">Join us today and start your journey 🚀</p>

      <!-- Form -->
      <form action="/signup" method="POST" class="space-y-5">
        
       <%- include('../partials/errors') %>
        <!-- First Name -->
        <div class="relative">
          <span class="absolute inset-y-0 left-3 flex items-center text-gray-400">
            <i class="fas fa-user"></i>
          </span>
          <input
            type="text"
            name="firstName"
            placeholder="First Name"
            value="<%= oldInput.firstName ? oldInput.firstName : '' %>"
            class="w-full pl-10 pr-4 py-3 border rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-red-400 focus:border-red-400"
            required />
        </div>

        <!-- Last Name -->
        <div class="relative">
          <span class="absolute inset-y-0 left-3 flex items-center text-gray-400">
            <i class="fas fa-user-tag"></i>
          </span>
          <input
            type="text"
            name="lastName"
            placeholder="Last Name"
            value="<%= oldInput.lastName ? oldInput.lastName : '' %>"
            class="w-full pl-10 pr-4 py-3 border rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-red-400 focus:border-red-400"/>
        </div>

        <!-- Email -->
        <div class="relative">
          <span class="absolute inset-y-0 left-3 flex items-center text-gray-400">
            <i class="fas fa-envelope"></i>
          </span>
          <input
            type="email"
            name="email"
            placeholder="xyz@pop.com"
            value="<%= oldInput.email ? oldInput.email : '' %>"
            class="w-full pl-10 pr-4 py-3 border rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-red-400 focus:border-red-400"
            required />
        </div>

        <!-- Password -->
        <div class="relative">
          <span class="absolute inset-y-0 left-3 flex items-center text-gray-400">
            <i class="fas fa-lock"></i>
          </span>
          <input
            type="password"
            name="password"
            placeholder="Enter Your Password"
            class="w-full pl-10 pr-4 py-3 border rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-red-400 focus:border-red-400"
            required/>
        </div>

        <!-- Confirm Password -->
        <div class="relative">
          <span class="absolute inset-y-0 left-3 flex items-center text-gray-400">
            <i class="fas fa-lock"></i>
          </span>
          <input
            type="password"
            name="confirmpassword"
            placeholder="Confirm Password"
            class="w-full pl-10 pr-4 py-3 border rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-red-400 focus:border-red-400"
            required/>
        </div>

        <!-- User Type -->
        <div>
          <p class="font-semibold text-gray-700 mb-2">User Type</p>
          <div class="flex items-center space-x-6">
            <label class="flex items-center space-x-2">
              <input type="radio" name="usertype" value="guest" id="guest" class="text-red-500 focus:ring-red-500" checked = "<%  oldInput.usertype === "guest" %>" >
              <span>Guest</span>
            </label>
            <label class="flex items-center space-x-2">
              <input type="radio" name="usertype" value="host" id="host" class="text-red-500 focus:ring-red-500"   checked = "<%  oldInput.usertype === "host" %>" >
              <span>Host</span>
            </label>
          </div>
        </div>

       

        <!-- Button -->
        <button type="submit" class="w-full bg-gradient-to-r from-red-500 to-pink-500 text-white py-3 rounded-xl font-semibold hover:scale-105 hover:shadow-lg transition transform duration-300 ease-in-out">
          <i class="fas fa-user-plus mr-2"></i> Register
        </button>

        <!-- Extra -->
        <p class="text-center text-sm text-gray-500 mt-4">Already have an account? 
          <a href="/login" class="text-red-500 font-semibold hover:underline">Login</a>
        </p>
      </form>
    </div>  
  </main>

  <!-- Font Awesome CDN for icons -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
</body>
</html>
