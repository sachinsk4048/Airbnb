<%- include('../partials/header') %>
</head>
<body>
  <%- include('../partials/nav') %>
  <main class="container mx-auto bg-white shadow-lg rounded-lg p-8 mt-10 max-w-xl">
    <h2 class="text-3xl text-red-500 font-bold text-center mb-6 shadow-2xl">
      Here are your favourite homes:
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
      <% favouritehomes.forEach(home => { %>
        <div class="bg-white rounded-xl overflow-hidden shadow-md hover:shadow-xl transition duration-300">
          <img src="<%= home.photo %>" alt="<%= home.homename %>" class="w-full h-48 object-cover">
          <div class="p-4 space-y-2">
            <h2 class="text-xl font-semibold text-gray-800"><%= home.homename %></h2>
            <p class="text-gray-600">💰 Price: ₹<%= home.price %></p>
            <p class="text-yellow-500">⭐ Rating: <%= home.rating %>/5</p>
            <form action="/fav-list/delete/<%= home._id %>" method="POST">
              <button class="mt-2 px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700 transition">
                Remove from Favourite
              </button>
            </form>
          </div>
        </div>
      <% }) %>
    </div>
  </main>
</body>
</html>
