<%- include('../partials/header') %>
</head>
<body class="bg-gradient-to-br from-blue-50 to-pink-50 min-h-screen text-gray-800 font-sans">
  <%- include('../partials/nav') %>

  <main class="max-w-4xl mx-auto mt-10 p-6 bg-white shadow-2xl rounded-2xl">

    <!-- Title -->
    <h1 class="text-3xl md:text-4xl font-bold text-center text-pink-600 mb-6 border-b pb-4 shadow-sm">
      🏠 <%= home.homename %>
    </h1>

    <!-- Card -->
    <div class="grid md:grid-cols-2 gap-6 items-center">
      
      <!-- photo -->
      <div class="rounded-xl overflow-hidden shadow-lg">
        <img src="<%= home.photo %>" alt="<%= home.homename %>" class="w-full h-64 object-cover hover:scale-105 transition duration-500">
      </div>

      <!-- Details -->
      <div class="space-y-4 p-2">
        <p class="text-xl font-semibold text-gray-700">
          💰 <span class="text-green-600">Price:</span> ₹<%= home.price %>
        </p>
        <p class="text-xl font-semibold text-gray-700">
          ⭐ <span class="text-yellow-500">Rating:</span> <%= home.rating %>/5
        </p>

        <button class="mt-4 w-full py-3 bg-gradient-to-r from-pink-500 to-red-500 text-white text-lg font-semibold rounded-lg hover:from-pink-600 hover:to-red-600 transition">
          Book Now
        </button>
      </div>
      <div class="flex justify-center text-center" >
       <%- include('../partials/favourites') %>
      </div>
    </div>
  </main>
</body>
</html>
