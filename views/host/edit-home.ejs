<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= editing ? 'Edit Home' : 'Add Home' %></title>
  <link rel="stylesheet" href="/output.css" />
</head>

<body class="min-h-screen bg-gradient-to-br from-red-50 via-white to-red-100 flex items-center justify-center px-3">

  <!-- Card -->
  <div class="w-full max-w-2xl bg-white rounded-2xl shadow-xl border border-red-100 p-6">

    <!-- Header -->
    <div class="text-center mb-5">
      <h1 class="text-2xl font-bold text-red-600">
        <%= editing ? 'Edit Home' : 'Register Home' %>
      </h1>
      <p class="text-sm text-gray-500">
        Add your property details below
      </p>
    </div>

    <!-- Form -->
    <form 
      action="/host/<%= editing ? 'edit-home' : 'add-home' %>" 
      method="POST" 
      enctype="multipart/form-data"
      class="space-y-4"
    >

      <input type="hidden" name="id" value="<%= editing ? home._id : '' %>">

      <!-- Home Name -->
      <div>
        <label class="block text-xs font-semibold text-gray-700 mb-1">
          Home Name
        </label>
        <input
          type="text"
          name="homename"
          value="<%= editing ? home.homename : '' %>"
          placeholder="Sunshine Villa"
          class="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:border-red-500 outline-none"
        />
      </div>

      <!-- Price + Rating -->
      <div class="grid grid-cols-2 gap-4">

        <div>
          <label class="block text-xs font-semibold text-gray-700 mb-1">
            Price
          </label>
          <input
            type="text"
            name="price"
            value="<%= editing ? home.price : '' %>"
            placeholder="₹ 10000"
            class="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:border-red-500 outline-none"
          />
        </div>

        <div>
          <label class="block text-xs font-semibold text-gray-700 mb-1">
            Rating
          </label>
          <input
            type="text"
            name="rating"
            value="<%= editing ? home.rating : '' %>"
            placeholder="4.5"
            class="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:border-red-500 outline-none"
          />
        </div>

      </div>

      <!-- Image + Location -->
      <div class="grid grid-cols-2 gap-4">

        <div>
          <label class="block text-xs font-semibold text-gray-700 mb-1">
            Image
          </label>
        <input
          type="file"
          name="photo"
          accept="image/jpg, image/jpeg, image/png"
          class="w-full rounded-lg border border-gray-300 px-2 py-1.5 text-xs bg-white focus:ring-1 focus:ring-red-500 focus:border-red-500 outline-none"
        />
        </div>

        <div>
          <label class="block text-xs font-semibold text-gray-700 mb-1">
            Location
          </label>
          <input
            type="text"
            name="location"
            value="<%= editing ? home.location : '' %>"
            placeholder="City, Area"
            class="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:border-red-500 outline-none"
          />
        </div>

      </div>

      <!-- Description -->
      <div>
        <label class="block text-xs font-semibold text-gray-700 mb-1">
          Description
        </label>
        <textarea
          name="discription"
          rows="3"
          placeholder="Short home description"
          class="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm focus:ring-1 focus:ring-red-500 focus:border-red-500 outline-none resize-none"
        ><%= editing ? home.discription : '' %></textarea>
      </div>

      <!-- Button -->
      <button
        type="submit"
        class="w-full bg-gradient-to-r from-red-500 to-red-600 text-white py-2.5 rounded-lg font-semibold text-sm hover:from-red-600 hover:to-red-700 transition"
      >
        <%= editing ? 'Update Home' : 'Add Home' %>
      </button>

    </form>
  </div>

</body>
</html>
